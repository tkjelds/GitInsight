namespace GitInsight.app
{
    class Program
    {
        private static void Main(string[] args)
        {
            Console.WriteLine("Hello world");
            // var _repo = args[0]; // Path til repo
            // var _mode = args[1]; //Frequence = 0 | Author = 1
            // Repository repo = new Repository(_repo); // lav repo object
            // repo.Commits.GroupBy( u => u.Author); // sorter på Author
        }
    }  
}
